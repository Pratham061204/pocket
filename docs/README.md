Place a screenshot of the running app here named `screenshot.png`.

Steps:
1. Open the live Vercel URL in a browser
2. Log in with the demo account
3. Take a screenshot of the Flatmates group balances page
4. Save it as `docs/screenshot.png`
5. Commit and push

The README.md at the root already references `docs/screenshot.png`.
